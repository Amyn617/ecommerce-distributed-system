spring:
  application:
    name: billing-service
  config:
    import: optional:configserver:http://localhost:9999
server:
  port: 8083
management:
  endpoints:
    web:
      exposure:
        include: "*"