spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      discovery:
        locator:
          lower-case-service-id:
            true # lowercase service name
#      routes:
#        - id: r1
#          uri: lb://CUSTOMER-SERVICE # Uppercase
#          predicates:
#            Path=/api/customers/**
#        - id: r2
#          uri: lb://INVENTORY-SERVICE
#          predicates:
#            Path=/api/products/**
    discovery:
      enabled: true
    config:
      enabled: false
server:
  port: 8888
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

    # 13:00 video 2